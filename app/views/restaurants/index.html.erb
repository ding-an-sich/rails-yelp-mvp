<h1>Restaurants</h1>
<ul>
<% @restaurants.each do |restaurant| %>
  <li>
    <%= link_to restaurant.name, restaurant_path(restaurant.id) %>
    <%= restaurant.category %>
  </li>
<% end %>
</ul>
<%= link_to 'Tell us about a new restaurant', new_restaurant_path %>

